import React from 'react';
import styles from './Main.module.scss'

const Main:React.FC = () => {
    return (
        <section className={styles.wrapper}>

        </section>
    );
};

export default Main;